% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{get_tehsils}
\alias{get_tehsils}
\title{Get Pakistan tehsil boundaries}
\usage{
get_tehsils(district = NULL, province = NULL, crs = 4326)
}
\arguments{
\item{district}{Character. Filter to one district by exact name.
Matching is case-insensitive. NULL returns all.}

\item{province}{Character. Filter to one province by exact name.
Matching is case-insensitive. NULL returns all.
If both district and province are supplied, district takes precedence.}

\item{crs}{Integer EPSG code. Default 4326 (WGS84). Use 32642 for
distance and area calculations. See \code{\link[=pk_crs_suggest]{pk_crs_suggest()}} for guidance.}
}
\value{
An sf object with columns: province_name, district_name,
tehsil_name, tehsil_code, area_km2, geometry.
}
\description{
Get Pakistan tehsil boundaries
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
  sindh_tehsils  <- get_tehsils(province = "Sindh")
  sindh_tehsils  <- get_tehsils(province = "sindh")  # Case-insensitive
  lahore_tehsils <- get_tehsils(district = "Lahore")
  lahore_tehsils <- get_tehsils(district = "lahore")  # Case-insensitive
\dontshow{\}) # examplesIf}
}
